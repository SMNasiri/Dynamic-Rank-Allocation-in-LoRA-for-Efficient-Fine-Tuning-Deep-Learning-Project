# Colab/reproducibility requirements.
# PEFT is pinned because the research extension subclasses its internal RankAllocator.
peft==0.20.0
datasets>=3.0
evaluate>=0.4
scikit-learn>=1.3
matplotlib>=3.8
pyyaml>=6.0
pytest>=8.0
sentencepiece>=0.2
